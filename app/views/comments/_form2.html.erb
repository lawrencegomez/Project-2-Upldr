<%= simple_form_for @comment do |f| %>

  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this image from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <p><%=@comment.body%></p>
    <%= link_to 'Yes', comments_path, class: 'btn btn-primary'%>
    <%= link_to "Cancel", comments_path, class: 'btn btn-primary' %>

<% end %>
