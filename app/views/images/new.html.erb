<%= form_for @image do |f| %>

  <%= f.label :title %>
  <%= f.text_area :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :image_url %>
  <%= f.text_area :image_url %>

  <%= f.label :tags %>
  <%= f.text_area :tags %>

  <%= f.submit %>
<% end %>
